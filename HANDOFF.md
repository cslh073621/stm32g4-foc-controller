# 项目 Handoff — BLDC/PMSM FOC 智能电机控制器

> 用途：作为项目跨会话、跨阶段的唯一进度交接文件。每次重要推进后更新本文件。
> 项目源基准：《最初目标.txt》v1.0。

## 1. 项目目标

构建一套基于 STM32G4 的 24V 低压 BLDC/PMSM 有感 FOC 智能电机控制器，并逐步完成电流环、速度环、FreeRTOS、CAN、上位机、故障管理、Bootloader、参数存储、日志及完整工程文档。

核心原则：
- 安全优先，只做 12–24V 低压直流系统。
- 严格按 Phase 推进，未验收不进入下一阶段。
- 解释 → 设计 → 少量代码 → 编译 → 运行 → 反馈 → 修正。
- 事实优先：编译结果、日志、波形、截图、源码、原理图和 datasheet 优先于推测。
- 高速 FOC 控制放在 PWM/ADC 同步 ISR 链路，RTOS 负责低速业务层。
- Git 从第一天开始，保留真实开发历史。

## 2. 当前阶段

**Phase 0：能力、设备与采购路线审计（2026-09-15 项目重启）**

新增现实约束：办公室环境不适合公开进行硬件开发，因此项目采用“办公室纯软件/仿真线 + 家里/周末硬件线”的双轨方式推进。

Phase 0 目标：
1. 建立当前知识能力表。
2. 建立当前可用开发设备表。
3. 建立缺少硬件表。
4. 建立开发环境表。
5. 建立第一阶段任务表。

## 3. 已知个人背景

- 嵌入式软件工程师，毕业约 2 年，正式工作约 2 年。
- 当前主要从事低复杂度小家电/风扇类 MCU 裸机开发。
- 过往 STM32 实际开发经验有限，但不是零基础；基础外设曾经做过，现阶段主要是需要复建手感与体系。
- 主要使用国产 SOP8 / SOP16 级基础 MCU；相对高级的 MCU 经验主要是 51 类或仿 51 架构。
- 已有基础：C、GPIO、Timer、PWM、ADC、UART、SPI、I2C、简单协议、状态机、基础外设驱动、产品调试。
- 不能默认已经熟悉 STM32 的时钟树、NVIC、中断优先级、DMA、高级定时器、ADC 触发链路、CubeMX/CubeIDE/HAL/LL 等生态。
- 当前主要短板：STM32 平台系统性经验、RTOS、FOC、电机控制、Bootloader/OTA、CAN、复杂软件架构、控制理论、功率电子、三相逆变、电流采样。

## 4. 当前已确定技术路线

- 最终主控路线：STM32G431RB；实体开发板首选官方 `NUCLEO-G431RB`。
- 不购买 STM32G431 裸芯片作为起步平台，也不优先购买来源不明的最小系统板。
- NUCLEO-G431RB 自带 ST-LINK，可直接用于下载和调试。
- 办公室优先采用 Proteus VSM + STM32 受支持型号做基础外设复建，以及纯 C 算法、架构、单元测试、文档和 Git。
- 当前 Proteus 官方 Cortex-M4 支持列表未列出 STM32G431/G4，因此不强行使用第三方 G431 仿真模型；基础复建优先选官方支持的 STM32F401 系列作为仿真目标。
- Proteus 仿真只用于基础 GPIO、UART、Timer、普通 PWM、ADC、部分 DMA/中断概念与外围电路联调；不把它视为 STM32G431 高级定时器、ADC 同步采样、真实中断时序或 FOC 功率级的等价替代。
- 真实 G431 专属能力，包括高级 Timer、Center-aligned PWM、Complementary PWM、Dead Time、Timer-triggered ADC、ADC+DMA、PWM/ADC 同步链路、ISR 时间等，必须在 NUCLEO-G431RB 上最终验证。
- 纯算法模块必须尽量写成“与 HAL/寄存器解耦”的普通 C 模块，以便办公室直接在 PC 上编译和测试，之后再接 STM32 BSP。
- RTOS：FreeRTOS。
- 通信：CAN 优先，UART 调试；后续可扩展 RS485/Modbus、CANopen。
- 上位机：Python + PySide6/PyQt + pyserial/CAN + pyqtgraph/matplotlib。
- 电机：小功率 BLDC/PMSM。
- 供电：12–24V，带限流功能。
- 功率级：初期优先成熟低压三相驱动板，不直接挑战高压/大功率逆变。

## 5. 今天完成

- 2026-09-15：项目正式重启。
- 重新确认最终主控平台为 STM32G431RB，实体板首选 `NUCLEO-G431RB`。
- 明确办公室不便摆放/连接硬件，因此采用双轨开发。
- 明确用户希望优先通过“可拖元器件、连线并加载单片机程序”的仿真软件恢复 STM32 基础。
- 已确认该软件路线为 Proteus VSM。
- 已确认当前 Proteus 官方 Cortex-M4 支持列表未列出 STM32G431/G4，因此办公室仿真优先采用官方支持的 STM32F401 系列做基础外设复建，而不将第三方 G431 模型作为项目依据。

## 6. 当前采购计划

### 第一批：建议采购，但不要求带到办公室
- `NUCLEO-G431RB` ×1（核心实体板，当前看到价格约 175 元；放家里即可）
- 匹配板卡接口的 USB 数据线 ×1（若卖家不附）
- 杜邦线若干（若手头没有）
- 面包板 ×1（若手头没有）
- 万用表 ×1（若手头没有）

### 第二批：完成 STM32 基础外设实验后再买
- `X-NUCLEO-IHM16M1` 三相电机驱动扩展板 ×1，或届时重新评估更合适的低压驱动板
- 小功率 12–24V BLDC/PMSM，优先带 Hall，具体参数需在购买前确认
- 0–30V / 3–5A 可调直流电源，必须支持恒流限流
- 示波器（建议至少 2 通道；具体型号和带宽在采购时再选）

### 第三批：项目需要时再买
- CAN 收发器模块
- 编码器或磁编码器
- 逻辑分析仪
- 温度传感器等扩展器件

暂不购买：
- STM32G431 裸芯片
- 不明来源 G431 最小系统板
- 大功率 ESC / 高压功率板
- 独立 ST-LINK（NUCLEO 已自带）

## 7. 双轨工作方式

### 办公室：Proteus / 纯软件线
适合做：
- STM32F401 Proteus 基础复建：GPIO、按键、LED、UART、Timer、中断、普通 PWM、ADC 等
- C 语言补缺
- 状态机
- PI 控制器
- Clarke / Park / inverse Park
- SVPWM 占空比算法
- 数值边界与饱和处理
- 纯 C 单元测试
- FOC 数据结构和模块接口设计
- 参数管理、Fault Manager、协议层设计
- FreeRTOS 概念、任务/队列/状态机设计（真实调度时序仍需后续验证）
- Git、README、docs、控制框图
- Python 上位机的离线 UI/协议开发

原则：仿真用于复建概念与代码组织，不把仿真结果直接当作 G431 实机结论。核心算法层不直接依赖 HAL。

### 家里 / 周末：真实硬件线
必须验证：
- G431 Clock Tree
- NVIC 与中断优先级
- GPIO / UART 实机
- Timer / PWM
- Center-aligned PWM
- Complementary PWM / Dead Time
- ADC sampling time
- Timer-triggered ADC
- ADC + DMA
- PWM/ADC 同步链路
- ISR 实际执行时间
- 六步换相
- Hall / Encoder
- 三相电流采样
- 开环 FOC
- 电流环 / 速度环
- 故障保护

## 8. 下一步

办公室优先建立 Proteus STM32F401 基础复建工程，不先碰 FOC。

建议复建顺序：
1. GPIO 输出 + LED
2. GPIO 输入 + 按键
3. UART + Virtual Terminal
4. Timer 中断
5. 普通 PWM + 虚拟示波器
6. ADC + 电位器
7. DMA / 中断概念实验（以 Proteus 能可靠支持的外围行为为准）

完成这些后，再转 NUCLEO-G431RB 做 G431 真机专项：高级 Timer、中心对齐、互补 PWM、死区、Timer→ADC、ADC+DMA、PWM/ADC 同步。

## 9. 需要保存的关键参数

当前已确认：
- 最终主控：STM32G431RB
- 首块实体开发板：NUCLEO-G431RB
- 当前实体板参考价格：约 175 元
- 办公室仿真工具：Proteus VSM
- 办公室仿真 MCU：优先 STM32F401 系列（基于 Proteus 官方支持列表）
- 开发方式：办公室仿真/纯软件 + 家里/周末硬件验证

后续持续记录：
- 电机额定电压、电流、极对数
- 驱动板型号
- 电流采样电阻与放大倍数
- PWM 频率
- ADC 触发点与采样时间
- 编码器分辨率 / Hall 状态表
- 电流环 PI
- 速度环 PI
- CAN 波特率与协议版本
- Bootloader 分区地址
- 固件版本

## 10. 每次会话结束必须更新

固定更新以下栏目：
- 【当前阶段】
- 【今天完成】
- 【存在问题】
- 【下一步】
- 【需要保存的关键参数】

只有实测、编译、日志、波形或明确确认过的内容才能写成“已完成”。
