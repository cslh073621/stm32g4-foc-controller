# 项目 Handoff — BLDC/PMSM FOC 智能电机控制器

> 用途：作为项目跨会话、跨阶段的唯一进度交接文件。每次重要推进后更新本文件。
> 项目源基准：《最初目标.txt》v1.0。

## 1. 项目目标

构建一套基于 STM32G4 的 24V 低压 BLDC/PMSM 有感 FOC 智能电机控制器，并逐步完成电流环、速度环、FreeRTOS、CAN、上位机、故障管理、Bootloader、参数存储、日志及完整工程文档。

核心原则：
- 安全优先，只做 12–24V 低压直流系统。
- 严格按 Phase 推进，未验收不进入下一阶段。
- 解释 → 设计 → 少量代码 → 编译 → 运行 → 反馈 → 修正。
- 事实优先：编译结果、日志、波形、截图、源码、原理图和 datasheet 优先于推测。
- 高速 FOC 控制放在 PWM/ADC 同步 ISR 链路，RTOS 负责低速业务层。
- Git 从第一天开始，保留真实开发历史。

## 2. 当前阶段

**Phase 0：能力与设备审计**

当前正在进行能力基线确认与首批硬件采购规划。

Phase 0 目标：
1. 建立当前知识能力表。
2. 建立当前可用开发设备表。
3. 建立缺少硬件表。
4. 建立开发环境表。
5. 建立第一阶段任务表。

## 3. 已知个人背景

- 嵌入式软件工程师，毕业约 2 年，正式工作约 2 年。
- 当前主要从事低复杂度小家电/风扇类 MCU 裸机开发。
- 过往 STM32 实际开发经验很少。
- 主要使用国产 SOP8 / SOP16 级基础 MCU；相对高级的 MCU 经验主要是 51 类或仿 51 架构。
- 已有基础：C、GPIO、Timer、PWM、ADC、UART、SPI、I2C、简单协议、状态机、基础外设驱动、产品调试。
- 因平台差异，虽然具备 MCU 裸机经验，但不能默认已经熟悉 STM32 的时钟树、NVIC、中断优先级、DMA、高级定时器、ADC 触发链路、CubeMX/CubeIDE/HAL/LL 等生态。
- 当前主要短板：STM32 平台经验、RTOS、FOC、电机控制、Bootloader/OTA、CAN、复杂软件架构、控制理论、功率电子、三相逆变、电流采样。

## 4. 当前已确定技术路线

- 第一块 MCU 学习/主控板优先：STM32G431，首选官方 NUCLEO-G431RB。
- 暂不购买 STM32G431 裸芯片，也不优先购买不明来源的最小系统板。
- 暂不急于购买高集成功率 ESC；先把 STM32 基础、PWM、ADC、Timer、DMA、中断和调试链路建立起来。
- RTOS：FreeRTOS。
- 通信：CAN 优先，UART 调试；后续可扩展 RS485/Modbus、CANopen。
- 上位机：Python + PySide6/PyQt + pyserial/CAN + pyqtgraph/matplotlib。
- 电机：小功率 BLDC/PMSM。
- 供电：12–24V，带限流功能。
- 功率级：初期优先成熟低压三相驱动板，不直接挑战高压/大功率逆变。

## 5. 今天完成

- 已确认 ChatGPT 项目源中的《最初目标.txt》可被读取，并作为项目规则基准。
- 已建立 GitHub 仓库 `cslh073621/stm32g4-foc-controller` 作为长期项目代码与文档仓库。
- 已建立并验证 `HANDOFF.md` 可由 ChatGPT 直接更新。
- 已补充真实能力基线：STM32 实战经验很少，主要经验来自国产 SOP8/SOP16 基础 MCU 与 51 类 MCU。
- 已据此调整采购策略：先解决 STM32 平台学习与调试，再进入电机功率级。

## 6. 存在问题

当前缺少以下实际信息：
- 手头是否已有可复用的开发板或调试器。
- 电源、示波器、逻辑分析仪、万用表等设备情况。
- 电机、驱动板、编码器/Hall 等硬件情况。
- 当前开发电脑上是否已有 STM32CubeIDE / CubeMX / Keil / Git 等环境。
- C 语言和 MCU 基础的实际能力还需后续通过小实验验证，不能只凭自述判断。

## 7. 下一步

当前优先完成首批硬件采购，不急于进入开发环境审计。

**首个明确动作：**
寻找并确认一块 `NUCLEO-G431RB`。优先原装 ST Nucleo 板，因为自带 ST-LINK，适合从 STM32 基础、调试、Timer/PWM/ADC/DMA 逐步推进。

第一批建议只采购：
- NUCLEO-G431RB ×1
- 常用杜邦线 / 面包板（如手头没有）
- 万用表（如手头没有）

电机、三相驱动板、示波器、可调电源根据已有设备情况再决定，不一次性全买。

## 8. 需要保存的关键参数

当前暂无已确认硬件关键参数。

后续需要持续记录：
- MCU / 开发板型号
- 电机额定电压、电流、极对数
- 驱动板型号
- 电流采样电阻与放大倍数
- PWM 频率
- ADC 触发点与采样时间
- 编码器分辨率 / Hall 状态表
- 电流环 PI
- 速度环 PI
- CAN 波特率与协议版本
- Bootloader 分区地址
- 固件版本

## 9. 每次会话结束必须更新

固定更新以下栏目：

- 【当前阶段】
- 【今天完成】
- 【存在问题】
- 【下一步】
- 【需要保存的关键参数】

只有实测、编译、日志、波形或明确确认过的内容才能写成“已完成”。
